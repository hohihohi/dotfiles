# Devide .zshrc file and read ~/.zsh.d directory
ZSHHOME="${HOME}/.zsh.d"

if [ -d $ZSHHOME -a -r $ZSHHOME -a \
  -x $ZSHHOME ]; then
  for i in "$ZSHHOME"/.*.zsh(N); do
    [[ -r $i ]] && source "$i"
  done
fi
